# Claude Code local machine settings (permissions, paths — machine-specific)
.claude/settings.local.json

# OS junk
Thumbs.db
Desktop.ini
.DS_Store

# Power BI / Office temp & lock files
~$*
*.tmp

# PBIP machine-local files (cache, user settings)
**/.pbi/localSettings.json
**/.pbi/cache.abf

# Environment / secrets (safety net)
.env
*.env
